"$schema" = "https://conductor.build/schemas/settings.repo.schema.json"

[scripts]
setup = "bash setup.sh"
run_mode = "nonconcurrent"

[scripts.run.dev]
command = "uv run prospector serve --dev"
default = true
icon = "play"
